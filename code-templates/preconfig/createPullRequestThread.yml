steps:
  - template: ../utils/thread-createComment.yml@Templates_Salesforce
    parameters:
      messageContent: "## Starting Build. Executing Pipeline: [$(Build.DefinitionName)]($(System.TeamFoundationCollectionUri)$(System.TeamProject)/_build/results?buildId=$(Build.BuildId)) ##"

  - task: Bash@3
    inputs:
      targetType: 'inline'
      script: |
        echo "##vso[task.setvariable variable=pullRequestThreadId]$(pr_thread_id)"
